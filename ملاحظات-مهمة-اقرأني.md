# ملاحظات مهمة لثيم Eloria — اقرأني قبل أي تعديل

## قاعدة التعامل مع رنيم (صاحبة الثيم)
**لا تعدّل أي شيء في الثيم من نفسك أبداً. اقترح التعديل أولاً واشرحه، ونفّذه فقط بعد موافقتها الصريحة.**

---

## ✅ تم — تعديلات منفّذة محلياً (بانتظار البناء والنشر)
- المكوّنات حُدّثت: Tranexamic Acid، Niacinamide، Glutathione، Panthenol، Glycerin، Propylene Glycol (الرئيسية + المنتج + من نحن + الترجمتين).
- روابط الجافاسكربت صُحّحت في كل الصفحات: asset('js/X.js') → 'X.js'|asset (الإصلاح المؤجّل أدناه صار منفّذاً).
- لم يُبنَ ولم يُنشر بعد — ينتظر قرار رنيم بخصوص توقيت النشر (الثيم تحت مراجعة سلة / جاهز للمناقشة).

## ⚠️ (سابقاً) إصلاح مؤجّل — صار منفّذاً الآن

**المشكلة:** روابط ملفات الجافاسكربت في صفحات الثيم مكتوبة بصيغة Remix القديمة
`{{ asset('js/app.js') }}` بينما البناء (webpack) يخرج الملفات في جذر public بدون مجلد js.
النتيجة المحتملة: التصميم يظهر منسّقاً، لكن التفاعلات لا تعمل (قائمة الجوال،
أداة الروتين، سحب صور المنتج).

**الإصلاح المقترح:** تغيير الروابط إلى الصيغة الصحيحة:
- `{{ asset('js/app.js') }}` ← `{{ 'app.js' | asset }}`
- `{{ asset('js/routine-tool.js') }}` ← `{{ 'routine-tool.js' | asset }}`
- `{{ asset('js/product.js') }}` ← `{{ 'product.js' | asset }}`

**الملفات المتأثرة:** جميع ملفات الصفحات في `src/views/pages/` التي تحتوي
`asset('js/` — أبرزها: index.twig، product/single.twig، product/index.twig،
cart.twig، thank-you.twig، page-about.twig، page-single.twig، loyalty.twig،
blog/*، brands/*، customer/*.

**متى يُقترح:** عند أول طلب تعديل على الثيم، أو إذا اشتكت بعد التفعيل أن
القائمة/أداة الروتين لا تستجيب.

---

## دورة أي تعديل مستقبلي (3 خطوات)
1. تعديل الملفات (باقتراح وموافقة).
2. `pnpm run production` — بناء.
3. `salla theme publish` — نشر (وصف إنجليزي ← e، وصف عربي ٢٥+ حرف ← e، build؟ yes).
   - إذا فشل الرفع بـ rejected: `git push -f origin master` ثم publish من جديد.

## معلومات المشروع
- Theme ID: 1216973000 — حساب الشركاء: portal.salla.partners/themes/1216973000
- المتجر: eloriapure.com — GitHub: github.com/rno1121/eloria
- pnpm فقط (لا npm) — متطلبات سلة الكاملة موثّقة في رسالة سابقة (مكوّنات salla-* الإلزامية، snake_case، features في twilight.json، حزم Twilight حديثة، سكرينشوت قبل النشر).
